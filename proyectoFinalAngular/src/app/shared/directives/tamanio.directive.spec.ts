import { TamanioDirective } from './tamanio.directive';

describe('TamanioDirective', () => {
  it('should create an instance', () => {
    const directive = new TamanioDirective();
    expect(directive).toBeTruthy();
  });
});
